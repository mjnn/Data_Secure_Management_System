---
description: 用户工作流偏好：信息不足时选项式追问；改动后重启前后端
alwaysApply: true
---

# 用户工作流偏好（本仓库）

## 1. 信息不足时主动澄清

当任务所需信息不完整、存在多种合理解读或关键约束未写明时：

- **先停手猜实现**，用简短问题列出缺口；
- **给出可选方案**（例如 A / B / C 或「是 / 否 / 其它：请说明」），便于用户一键选择或微调；
- 选项应互斥或边界清晰，避免开放式泛泛提问。

与 `karpathy-guidelines.mdc` 中「不要默默假设」一致；本规则强调**结构化选项**以加快补充信息。

## 2. 代码或配置改动后重启前后端

在完成**会影响运行态**的改动后（业务代码、API、环境变量、依赖安装等），须保证用户侧验证基于**新进程**：

- **前端**：在 `frontend` 目录执行 `pnpm run dev`（默认 `http://127.0.0.1:5173/`）。
- **后端**：在 `backend` 目录、已安装依赖的解释器下执行  
  `python -m uvicorn app.main:app --host 127.0.0.1 --port 8000`。

若开发服务由 Agent 在终端中启动，Agent 应在合适时**停止旧进程并重新启动**上述两端。若服务仅在用户本地其它终端长期运行且 Agent 无法代停，须在回复中**明确提示用户手动重启前端与后端**，并简要说明与本次改动相关的原因。

纯文档、注释、与运行态无关的静态资源路径等可不强制重启；一旦涉及可执行代码或加载配置，按上文处理。
